mixin product-item(item)
  .product-item
    .inner-image
      a(href=`/book/detail/${item.id}`)
        img(
          alt="" 
          src=item.avatar
          )
    .inner-content
      h3.inner-title
        a(href="") #{item.name}
      .inner-prices
        .inner-price-new
          | Giá: #{item.priceBook.toLocaleString('vi-VN')}
          span.inner-unit đ
      .inner-meta
        .inner-stock
          .inner-label Số lượng còn:
          .inner-number #{item.numberBook}